import { PALETTE } from '../constants'

export const outlinedInputTheme = {
    root: {
        borderRadius: '16px',
        backgroundColor: PALETTE.WHITE.main,
        fontSize: 18,
        lineHeight: 28
    }
}
